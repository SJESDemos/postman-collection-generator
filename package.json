{
  "name": "postman-collection-generator",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix webapp",
    "build:backend": "npm run build --prefix backend",
    "build:web": "npm run build --prefix webapp",
    "cli": "npm run cli --prefix backend --",
    "serve": "npm run serve --prefix backend --",
    "test": "npm test --prefix backend",
    "verify": "npm test --prefix backend && npm run build --prefix webapp"
  },
  "engines": {
    "node": ">=24"
  }
}
